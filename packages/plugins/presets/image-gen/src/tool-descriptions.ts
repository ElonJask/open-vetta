export const GENERATE_IMAGE_TOOL_DESCRIPTION =
	"Generate a new image whenever the user asks to create, draw, illustrate, design, render, or produce an image, illustration, game asset, character art, item art, sticker, icon, or transparent-background asset. Trigger this tool when the requested deliverable is an actual newly generated picture or standalone visual asset, including a generated reference image, concept art, game material, transparent image, or similar artwork. This includes brief requests in any language. Call this tool for the actual visual deliverable; do not only describe a prompt in text.\nBefore calling, translate and expand the user's request into a detailed English prompt covering the subject, style, lighting, composition, background or transparency, and requested constraints. The tool forwards that prompt and does not optimize it for you. Use `edit_image` only when modifying an existing image.\nDo NOT use this tool when the user asks to build or implement a website, web page, application, UI design, UI screen, UI rendering, prototype, HTML/CSS mockup, diagram, chart, or other deterministic software artifact. Implement those with code or the appropriate design tool instead. Do not infer image generation merely because a webpage or UI needs visuals; use this tool only when the user explicitly requests a standalone generated image asset. Every call is billed and cannot be un-billed.";

export const EDIT_IMAGE_TOOL_DESCRIPTION =
	"Edit a specific existing image. Use sourceImageId for a previously generated image or sourceImagePath for a local file, never both. Every call is billed and cannot be un-billed.\nDo NOT use for deterministic image work — cropping, resizing, format conversion, compositing or annotating a screenshot; use an image-processing tool or code for those instead.\nOnly for a generative change to the picture itself, asked for by the user.";
